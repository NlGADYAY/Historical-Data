import styled from "styled-components";

export const KinoConteiner = styled('div')({
    '@media (max-width: 320px)': {
        width: '100%',
        height: '100%',
        paddingBottom: '70px'
    }
})